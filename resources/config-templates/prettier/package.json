{
  "name": "monoco-prettier-config",
  "version": "1.0.0",
  "description": "Prettier configuration for Monoco projects",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.2.0"
  }
}
