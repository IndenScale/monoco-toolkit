# Monoco Issue System 用户手册

Monoco Issue System 是一个基于 **"Agent-Native Semantics"** (智能体原生语义) 构建的项目管理工具。它将工作单元视为“通用原子”(Universal Atoms)，旨在减少人类意图与智能体执行之间的翻译损耗。

本手册将详细介绍 Issue System 的核心概念、工作流以及命令行工具的使用方法。

## 1. 核心概念 (Core Ontology)

Monoco 摒弃了传统的“用户叙事”(User Narrative) 模式，转而基于“思维模式”(Mindset) 和“直接功能”(Direct Functionality) 来定义工作单元。

### 1.1 战略层 (Strategy Layer)

#### 🏆 EPIC (史诗)

- **思维模式**: 架构师 (Architect)
- **定义**: 跨越多个周期的宏大目标，是“愿景的容器”。
- **产出**: 定义系统的边界与核心价值。
- **前缀**: `EPIC-`

### 1.2 价值层 (Value Layer)

#### ✨ FEATURE (特性)

- **思维模式**:由于 Product Owner / 首席工程师
- **定义**: 系统中具体的、功能性的单元。代表 **价值交付**。
- **关注点**: "What" (增加了什么功能？)
- **原子性**: Feature = Design + Dev + Test。它们是一个不可分割的整体。
- **前缀**: `FEAT-`
  > _注意：此概念替代了传统的 "Story" (用户故事)。_

### 1.3 执行层 (Execution Layer)

#### 🧹 CHORE (杂务)

- **思维模式**: 构建者 (Builder) / 维护者 (Maintainer)
- **定义**: 工程维护、重构或工具升级。这些工作 **不直接产生** 用户功能价值，但对系统健康至关重要。
- **场景**: 架构升级、编写构建脚本、修复 CI/CD 流水线、依赖库更新。
- **关注点**: "How" (为了支撑系统运行需要做什么)。
- **前缀**: `CHORE-`
  > _注意：此概念替代了传统的 "Task" (任务)。_

#### 🐛 FIX (修复)

- **思维模式**: 调试者 (Debugger)
- **定义**: 修正“预期”与“现实”之间的偏差。
- **关注点**: "Repair" (恢复到预期状态)。
- **前缀**: `FIX-`
  > _注意：此概念替代了传统的 "Bug" (缺陷)。_

### 关系链

- **主线**: `EPIC` (愿景) -> `FEATURE` (价值交付)
- **支线**: `CHORE` (技术维护) - 可作为独立的工程任务存在。
- **职能**: Feature 交付价值；Chore 维护工厂；Fix 修复破窗。

---

## 2. 命令行参考 (Command Reference)

Monoco Issue System 强烈推荐使用 CLI 工具来管理 Issue，以确保元数据与文件系统的同步。

### 2.1 创建 (Create)

创建新的 Issue。会自动分配下一个可用的 ID。

```bash
monoco issue create <type> --title "标题" [options]
```

- **参数**:
  - `<type>`: `epic`, `feature`, `chore`, `fix`
  - `--title, -t`: Issue 的标题。
  - `--parent, -p`: 父级 Issue ID (例如关联 Feature 到 Epic)。
  - `--dependency, -d`: 依赖的 Issue ID。
  - `--related, -r`: 关联的 Issue ID。
  - `--sprint`: 指定 Sprint ID。
  - `--tag`: 添加标签。
  - `--backlog`: 直接创建在 Backlog 状态（默认为 Open）。
  - `--subdir, -s`: 指定子目录（用于组织结构，例如 `Backend/Auth`）。

**示例**:

```bash
monoco issue create feature --title "实现用户登录" --parent EPIC-001
```

### 2.2 状态流转 (Transition)

#### 开启 (Open)

将 Issue 移动到 `open` 状态。

```bash
monoco issue open <issue_id>
```

#### 搁置 (Backlog)

将 Issue 移动到 `backlog` 状态。

```bash
monoco issue backlog <issue_id>
```

#### 提交 (Commit)

原子化提交 Issue 变更。此命令会自动进行 Lint 检查，并仅提交 Issues 目录下的变更。

```bash
monoco issue commit [-m "message"]
```

#### 关闭 (Close)

完成或关闭 Issue。

**注意**: 若 Solution 为 `implemented`（默认），Issue 必须处于 `Review` 阶段（需先运行 `submit`）。

```bash
monoco issue close <issue_id> [--solution <type>]
```

- **--solution**: 指定关闭原因。可选值:
  - `implemented` (已实现 - 默认)
  - `cancelled` (已取消)
  - `wontfix` (不予修复)
  - `duplicate` (重复)

#### 取消 (Cancel)

快捷命令，等同于 `close --solution cancelled`。

```bash
monoco issue cancel <issue_id>
```

### 2.3 查看与可视化 (Scope)

以树状结构查看当前项目的 Issue 进展。

```bash
monoco issue scope [options]
```

- **--all, -a**: 显示所有 Issue（包含 Closed 和 Backlog）。默认仅显示 Open。
- **--sprint**: 按 Sprint ID 过滤。
- **--recursive, -r**: 递归扫描子目录。

### 2.4 生命周期 (Lifecycle)

#### 开始 (Start)

开始处理一个 Issue (Stage -> Doing)。

```bash
monoco issue start <issue_id>
```

#### 提交 (Submit)

提交 Issue 以供评审 (Stage -> Review)。

```bash
monoco issue submit <issue_id>
```

### 2.5 维护 (Maintenance)

#### 删除 (Delete)

物理删除 Issue 文件。**警告**: 此操作不可逆。

```bash
monoco issue delete <issue_id>
```

#### 质量检查 (Lint)

验证 `Issues/` 目录的完整性。检查 ID 冲突、文件位置错误、死链等问题。

```bash
monoco issue lint [--recursive]
```

---

## 3. 最佳实践 (Best Practices)

### 3.1 目录结构

Issue 系统采用 **"Type-first, Status-second"** (类型优先，状态次之) 的分层存储策略：

```text
Issues/
├── Epics/
│   ├── open/
│   └── closed/
├── Features/
│   ├── open/
│   └── ...
└── ...
```

### 3.2 核心原则

1. **优先使用 CLI**: 尽量避免手动移动文件，以防止元数据（Frontmatter）与物理路径不一致。
2. **强制校验**: 如果你手动编辑了 Markdown 文件，请务必运行 `monoco issue lint` 确保没有破坏数据完整性。
3. **原子性交付**: Feature 是交付的最小单元。不要将 Feature 拆分为子 Task 文件，相关的 Design/Dev/Test 任务应作为 Feature 内部的 Checklist (`- [ ]`) 管理。

### 3.3 模板规范

所有 Issue 文件必须包含以下 Frontmatter：

```yaml
---
id: FEAT-0123
type: feature
status: open
title: "功能标题"
created_at: YYYY-MM-DD
tags: [tag1]
---
```
