{
  "name": "monoco-vscode",
  "displayName": "Monoco",
  "description": "Native Kanban & Agent Management for Monoco. Manage tasks, visualize workflows, and orchestrate AI agents directly in VS Code.",
  "version": "0.3.12",
  "publisher": "indenscale",
  "repository": {
    "type": "git",
    "url": "https://github.com/IndenScale/Monoco.git",
    "directory": "Toolkit/extensions/vscode"
  },
  "homepage": "https://github.com/IndenScale/Monoco/tree/main/Toolkit/extensions/vscode",
  "bugs": {
    "url": "https://github.com/IndenScale/Monoco/issues"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "monoco",
    "kanban",
    "project management",
    "issue tracking",
    "agent",
    "ai",
    "workflow",
    "productivity",
    "visualization",
    "todo",
    "markdown",
    "lsp"
  ],
  "icon": "media/icon.png",
  "activationEvents": [
    "onLanguage:markdown",
    "workspaceContains:.monoco/project.yaml",
    "workspaceContains:Issues"
  ],
  "main": "./client/out/client/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "monoco.helloWorld",
        "title": "Monoco: Hello World"
      },
      {
        "command": "monoco.openBoard",
        "title": "Monoco: Open Kanban Board",
        "icon": "$(layout-sidebar-left)"
      },
      {
        "command": "monoco.createIssue",
        "title": "New Issue",
        "icon": "$(plus)"
      },
      {
        "command": "monoco.openSettings",
        "title": "Settings",
        "icon": "$(gear)"
      },
      {
        "command": "monoco.openCockpit",
        "title": "Monoco: Open Cockpit",
        "icon": "$(dashboard)"
      },
      {
        "command": "monoco.refreshEntry",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "monoco.bootstrap",
        "title": "Monoco: Install/Update Toolkit"
      },
      {
        "command": "monoco.selectProject",
        "title": "Select Project",
        "icon": "$(folder)"
      },
      {
        "command": "monoco.searchIssues",
        "title": "Search Issues",
        "icon": "$(search)"
      },
      {
        "command": "monoco.refreshTreeView",
        "title": "Refresh Issues",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "monoco-explorer",
          "title": "Monoco",
          "icon": "./media/icon.svg"
        }
      ]
    },
    "views": {
      "monoco-explorer": [
        {
          "id": "monoco.issueTreeView",
          "name": "Issues",
          "icon": "$(list-unordered)",
          "when": "config.monoco.useNativeTree != false"
        },
        {
          "type": "webview",
          "id": "monoco.issueFilterView",
          "name": "Filters",
          "icon": "$(filter)",
          "when": "config.monoco.useNativeTree != false"
        },
        {
          "type": "webview",
          "id": "monoco.kanbanView",
          "name": "Project Board (Legacy)",
          "icon": "$(layout-kanban)",
          "when": "config.monoco.useNativeTree == false"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "monoco.createIssue",
          "when": "view == monoco.kanbanView || view == monoco.issueTreeView",
          "group": "navigation@1"
        },
        {
          "command": "monoco.openSettings",
          "when": "view == monoco.kanbanView || view == monoco.issueTreeView",
          "group": "navigation@2"
        },
        {
          "command": "monoco.refreshEntry",
          "when": "view == monoco.kanbanView || view == monoco.issueTreeView",
          "group": "navigation@3"
        }
      ]
    },
    "configuration": {
      "title": "Monoco",
      "properties": {
        "monoco.executablePath": {
          "type": "string",
          "description": "Path to the Monoco CLI executable.",
          "scope": "machine-overridable",
          "default": "monoco"
        },
        "monoco.webUrl": {
          "type": "string",
          "description": "URL of the Monoco Web UI (Cockpit).",
          "default": "http://127.0.0.1:8642"
        },
        "monoco.debug": {
          "type": "boolean",
          "description": "Enable debug logging.",
          "default": false
        },
        "monoco.useNativeTree": {
          "type": "boolean",
          "description": "Use native TreeView instead of Webview for issue list (recommended for better drag & drop support).",
          "default": true
        },
        "monoco.cockpit.runtime.provider": {
          "type": "string",
          "enum": ["kimi", "vertex-ai", "openai", "anthropic", "local"],
          "default": "kimi",
          "description": "Agent provider (kernel) - Backend service driving the agent"
        },
        "monoco.cockpit.runtime.role": {
          "type": "string",
          "enum": ["principal-architect", "senior-engineer", "qa-specialist", "devops-engineer", "security-analyst", "product-manager", "default"],
          "default": "senior-engineer",
          "description": "Agent role (persona) - Defines expertise and behavioral characteristics"
        },
        "monoco.cockpit.runtime.autonomy.level": {
          "type": "string",
          "enum": ["yolo", "step-by-step", "full-manual"],
          "default": "yolo",
          "description": "Human-in-the-loop mode - Auto-approval behavior"
        },
        "monoco.cockpit.runtime.autonomy.persistence": {
          "type": "string",
          "enum": ["unlimited", "session", "task"],
          "default": "unlimited",
          "description": "Execution scope and time limits"
        },
        "monoco.cockpit.capabilities.skills.directory": {
          "type": "string",
          "default": "~/.monoco/skills",
          "description": "Directory containing skill packages"
        },
        "monoco.cockpit.capabilities.skills.sets": {
          "type": "array",
          "default": [],
          "description": "Enabled skill sets"
        },
        "monoco.cockpit.capabilities.systemAccess.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Bash-as-Tool system access"
        },
        "monoco.cockpit.capabilities.systemAccess.allowNetwork": {
          "type": "boolean",
          "default": true,
          "description": "Allow network access (HTTP requests, API calls)"
        },
        "monoco.cockpit.capabilities.systemAccess.allowFileSystem": {
          "type": "boolean",
          "default": true,
          "description": "Allow file system access (read, write, manipulate files)"
        },
        "monoco.cockpit.capabilities.systemAccess.allowSystemCommands": {
          "type": "boolean",
          "default": true,
          "description": "Allow system command execution (git, npm, docker, etc.)"
        },
        "monoco.cockpit.capabilities.systemAccess.restrictedCommands": {
          "type": "array",
          "default": ["rm -rf /", "dd", "mkfs"],
          "description": "Commands requiring explicit confirmation"
        },
        "monoco.cockpit.culture.language": {
          "type": "string",
          "enum": ["auto", "en", "zh"],
          "default": "auto",
          "description": "Communication language"
        },
        "monoco.cockpit.culture.tone": {
          "type": "string",
          "enum": ["professional", "technical", "casual"],
          "default": "professional",
          "description": "Communication tone"
        },
        "monoco.cockpit.culture.responseStyle": {
          "type": "string",
          "enum": ["concise", "balanced", "detailed"],
          "default": "balanced",
          "description": "Response detail level"
        }
      }
    },
    "languages": [
      {
        "id": "monoco-issue",
        "extensions": [
          ".md"
        ],
        "filenamePatterns": [
          "issues/*.md",
          "**/Issues/**/*.md"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "monoco-issue",
        "scopeName": "source.monoco-issue",
        "path": "./syntaxes/monoco-issue.tmLanguage.json"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -b shared/tsconfig.json && tsc -b client/tsconfig.json && tsc -b server/tsconfig.json && node client/scripts/resolve-aliases.js && node client/scripts/build-webview.js && node client/scripts/build-cockpit.js && npm run copy-assets && npm run copy-server-assets",
    "copy-assets": "node scripts/copy-assets.js",
    "copy-server-assets": "node scripts/copy-server-assets.js",
    "watch": "tsc -b client/tsconfig.json -w",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint client/src --ext ts",
    "format": "prettier --write .",
    "test": "node ./client/out/test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "20.x",
    "@types/sinon": "^10.0.0",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.14.1",
    "@vscode/test-electron": "^2.4.0",
    "@vscode/vsce": "^2.22.0",
    "eslint": "^8.57.0",
    "glob": "^10.3.10",
    "mocha": "^10.4.0",
    "ovsx": "^0.8.3",
    "prettier": "^3.4.2",
    "sinon": "^17.0.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.0"
  }
}
